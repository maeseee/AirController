# Used for terminal inputs (e.g. IDE started from terminal)
echo "export weather_api_key=<api_key>" >> ~/.bashrc
echo "export qingping_app_secret=<secret>" >> ~/.bashrc
echo "export mariaDdPassword=<secret>" >> ~/.bashrc

#Used for UI inputs (e.g. IDE started from UI)
echo "export weather_api_key=<api_key>" >> ~/.profile
echo "export qingping_app_secret=<secret>" >> ~/.profile
echo "export mariaDdPassword=<secret>" >> ~/.profile