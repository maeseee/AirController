sudo docker ps -a # Lists containers
sudo docker logs -f air_controller-app # see log output from the container
sudo docker exec -it air_controller-app /bin/bash
sudo docker images # Lists all images
